version: "3.8"

services:
  flask_app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"

#version: "3.8"
#
#services:
#  flask_app:
#    image: rag-core
#    ports:
#      - "5000:5000"
